# Shared Supabase project (same as icare_mc / icare_doctors).
# Copy to `.env.local` and fill in values from the MC or DR app `.env`.
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
